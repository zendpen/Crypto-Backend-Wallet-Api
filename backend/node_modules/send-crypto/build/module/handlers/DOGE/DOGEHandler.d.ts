import { UTXO } from "../../lib/utxo";
export declare const _apiFallbacks: {
    fetchUTXO: (testnet: boolean, txHash: string, vOut: number) => any[];
    fetchUTXOs: (testnet: boolean, address: string, confirmations: number) => any[];
    fetchTXs: (testnet: boolean, address: string, confirmations?: number) => any[];
    broadcastTransaction: (testnet: boolean, hex: string) => any[];
};
export declare class DOGEHandler {
    static getUTXOs: (testnet: boolean, options: {
        address: string;
        confirmations?: number;
    }) => Promise<readonly UTXO[]>;
    static getUTXO: (testnet: boolean, txHash: string, vOut: number) => Promise<UTXO>;
    static getTransactions: (testnet: boolean, options: {
        address: string;
        confirmations?: number;
    }) => Promise<readonly UTXO[]>;
}
