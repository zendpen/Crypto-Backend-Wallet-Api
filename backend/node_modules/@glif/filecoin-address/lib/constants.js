var generateProtocol = function generateProtocol(protocol) {
  if (protocol < 0 || protocol > 3) throw new Error('Invalid protocol');
  var protocolByte = new Buffer.alloc(1);
  protocolByte[0] = protocol;
  return protocolByte;
};

var ID = generateProtocol(0);
var SECP256K1 = generateProtocol(1);
var Actor = generateProtocol(2);
var BLS = generateProtocol(3);
module.exports = {
  ID: ID,
  SECP256K1: SECP256K1,
  Actor: Actor,
  BLS: BLS
};
//# sourceMappingURL=constants.js.map